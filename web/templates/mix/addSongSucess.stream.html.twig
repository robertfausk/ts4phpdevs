<turbo-stream action="update" target="track-list-edit">
    {# update replace remove append prepend before after #}
    <template>
        {{ include('mix/_trackList.html.twig', {mix: mix}) }}
    </template>
</turbo-stream>

<turbo-stream action="append" target="toast-container">
    <template>
        {% embed '_toast.html.twig' %}
            {% block toast_body %}
                Track added! Nice!
            {% endblock %}
        {% endembed %}
    </template>
</turbo-stream>